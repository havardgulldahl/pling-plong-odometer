tags:
- status
summary: Get a list of currently suppported resolvers and their file patterns
description: Use this to check whwat music resolvers are active, and what their file names look like
operationId: handle_supported_resovers
produces:
- application/json
responses:
  '200': 
    description: 'Current resolvers are listed'