tags:
- support
summary: Get a list of currently suppported resolvers and their file patterns
description: Use this to check which music resolvers are active, and what the file names they recognise look like
operationId: handle_supported_resovers
produces:
- application/json
responses:
  '200': 
    description: 'Current resolver in a list'